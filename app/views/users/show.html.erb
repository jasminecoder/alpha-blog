<h3 class="text-center">Welcome to <%= @user.username %>'s page</h3>
<div class="row">
  <div class="col-sm-4 col-sm-offset-4 center mx-auto d-block">
    <%= gravatar_for @user, size: 150 %>
  </div>
</div>
<h4 class="text-center pt-2"><%= @user.username %>'s articles</h4>
<%= render 'articles/article', obj: @user.articles %>
